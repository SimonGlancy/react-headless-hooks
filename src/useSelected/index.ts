export type {
  UseSelectedProps,
  UseSelectedReturnValues,
  SelectedObject,
  SelectFunction,
} from './useSelected.types';
export { SelectTypeEnum } from './useSelected.types';
export { default } from './useSelected';

export { normaliseData, getNewSelectedIndexes } from './utils';
